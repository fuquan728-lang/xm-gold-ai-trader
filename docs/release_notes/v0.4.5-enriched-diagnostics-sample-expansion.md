# v0.4.5 Enriched Diagnostics Sample Expansion

Release checkpoint for `xm-gold-ai-trader`, focused on documenting the expanded
read-only live dry-run sample set and the current enriched diagnostics coverage.

This checkpoint does not change trading logic, strategy thresholds, safety
checks, order routing, or AI annotation behavior.

## Scope

- Recorded the expanded live dry-run sample coverage after additional enriched
  journal observations.
- Kept old live dry-run journals compatible with the current quality analysis.
- Preserved the read-only research boundary: no order sending, no order check,
  no AI trading, no martingale, no grid, and no lot increase after loss.
- Kept the post-review reporting clarification: final `SIGNAL` outcomes and
  candidate BUY/SELL signals are reported separately.

## Current Sample Findings

- total valid journals: `129`
- unique closed bars: `121`
- enriched SMA diagnostic recorded count: `20`
- `fast_sma` recorded count: `20`
- `slow_sma` recorded count: `20`
- `previous_fast_sma` recorded count: `20`
- `previous_slow_sma` recorded count: `20`
- top failed pre-signal condition:
  - `SMA_CROSSOVER_NOT_PRESENT`: `120`
- top block reasons:
  - `NO_ACTIONABLE_SIGNAL`: `120`
  - `LOT_BELOW_VOLUME_MIN`: `1`
- final signal count: `0`
- zero final signal attribution remains `DOMINANT_RULE`, dominated by
  `NO_ACTIONABLE_SIGNAL`.

## Reporting Notes

- `actionable_signal_rate` now means final `SIGNAL` rate.
- Candidate BUY/SELL signals blocked by feasibility or risk gates are tracked
  separately as candidate signals.
- Current `research_pipeline_verify.py --json` is expected to return `WARN`
  while final signal count remains zero:
  - `ZERO_ACTIONABLE_SIGNAL_RATE`
  - `LIVE_SIGNAL_COUNT_ZERO`
- These warnings are research quality signals, not safety violations.

## Safety Status

- Default `configs/xm_gold_ai_trader.demo.yaml` remains
  `execution.allow_order_send: false`.
- `orders_sent` remains `0`.
- `order_check_called` remains `false`.
- `order_send_called` remains `false`.
- Phase 3 research and AI scripts remain read-only.
- AI annotation remains commentary-only and cannot alter trading decisions.

## Verification Commands

```powershell
python -m pytest
python scripts\analyze_live_sample_quality.py --json
python scripts\research_pipeline_verify.py --json
```
