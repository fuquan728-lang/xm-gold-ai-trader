# v0.4.6 Verifier Warning Taxonomy

Release checkpoint for `xm-gold-ai-trader`, focused on read-only verifier and
reporting clarity.

This checkpoint does not change trading logic, strategy thresholds, safety
checks, order routing, or AI annotation behavior.

## Scope

- Added warning taxonomy fields to `scripts/research_pipeline_verify.py --json`:
  - `safety_warnings`
  - `live_sample_coverage_warnings`
  - `research_quality_warnings`
  - `strategy_signal_warnings`
  - `warning_taxonomy`
- Kept `final_decision: WARN` when zero final actionable signal warnings are
  present.
- Made `ZERO_ACTIONABLE_SIGNAL_RATE` explicit as a read-only strategy signal
  quality warning, not a safety violation.
- Kept live sample coverage separate from strategy signal quality.

## Current Classification

- Safety warnings: none.
- Live sample coverage warnings: none.
- Research quality warnings:
  - `LIVE_SIGNAL_COUNT_ZERO`
- Strategy signal warnings:
  - `ZERO_ACTIONABLE_SIGNAL_RATE`

The current live sample coverage remains sufficient:

- unique closed bars: `121`
- required minimum closed bars: `100`

The current zero-final-signal state remains a research/strategy signal quality
warning:

- final `SIGNAL` count: `0`
- zero final signal attribution: `DOMINANT_RULE`
- dominant reason: `NO_ACTIONABLE_SIGNAL`

## Safety Status

- Default `configs/xm_gold_ai_trader.demo.yaml` remains
  `execution.allow_order_send: false`.
- `orders_sent` remains `0`.
- `order_check_called` remains `false`.
- `order_send_called` remains `false`.
- Phase 3 research and AI scripts remain read-only.
- AI annotation remains commentary-only and cannot alter trading decisions.

## Verification Commands

```powershell
python -m pytest
python scripts\research_pipeline_verify.py --json
python scripts\analyze_live_sample_quality.py --json
```
