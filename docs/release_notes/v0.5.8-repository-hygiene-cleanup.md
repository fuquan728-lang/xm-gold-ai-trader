# v0.5.8 Repository Hygiene Cleanup

Low-risk repository hygiene checkpoint. No production behavior changes.

## Scope

Drained stale/broken legacy references and reduced repository confusion
without changing any trading logic, strategy thresholds, risk settings, order
routing, or AI annotation behavior.

## Changes

### Phase 1 — Stale Document References

- Moved `系统优化升级总结.md` → `docs/archive/obsolete/` (refers to deleted `core/ai_engine_enhanced.py`)
- Moved `innovative_features.md` → `docs/archive/obsolete/` (refers to deleted `core/mql5_finance_data.py`)
- Moved 6 `docs/05-技术报告/` files → `docs/archive/obsolete/` (all refer to deleted `ai_file_server.py` or `ai_file_server_optimized.py`)
- Created `docs/archive/obsolete/README.md` with inventory of all archived documents

### Phase 2 — Broken Legacy Tests

- Created `legacy_v4/tests/README.md` documenting all 14 tests: 10 legacy, 1 broken (`test_validation.py` imports deleted module), 3 migratable
- Confirmed `pytest.ini` already excludes `legacy_v4` from default test suite

### Phase 3 — MQL5 EA Version Inventory

- Created `docs/MQL5_EA_VERSION_INVENTORY.md` documenting all EA versions:
  - Active: V3.2, V3.10, SafetyGuard, TradeCommandEA, GOLD_Short_EA, Advanced_GOLD_EA
  - Legacy: V2.1_Safe, Integrated_Optimized, Integrated_Socket
  - Archive candidates identified for next cleanup cycle
- **No EA files deleted** — only status documented

### Phase 4 — Obvious Trash Quarantine

- Moved `run_test_capture.py` → `archive/obsolete_tools/` (broken path reference)
- Moved `remove_emoji.py` → `archive/obsolete_tools/` (hardcoded old path)
- Moved 5 root-level historical Chinese reports → `docs/archive/obsolete/`
- Created `archive/obsolete_tools/README.md`

### Phase 5 — Integration Backlog

- Created `docs/backlog/repository_integration_candidates.md` listing:
  - `core/risk_manager.py` vs `src/strategy/risk_manager.py`
  - `core/trade_executor.py` vs `src/broker/order_executor.py`
  - WebSocket handler shim
  - Finance data module overlap
  - MQL5 data push modules
  - 3 migratable tests
- **No merges performed** — backlog only

## Impact

| Category | Count |
|----------|-------|
| Files moved to archive | 17 |
| New documentation | 5 files |
| EA files deleted | 0 |
| Strategy/risk/safety changes | 0 |
| Order routing changes | 0 |

## Safety Guarantees

- `orders_sent: 0`
- `order_check_called: false`
- `order_send_called: false`
- No production behavior changes
- All moved files are documentation or broken scripts — no active code affected

## Verification

```powershell
python -m pytest
python scripts\strategy_hypothesis_lab.py --json
python scripts\research_pipeline_verify.py --json
```

Expected: all pass with same WARNs as v0.5.7.
