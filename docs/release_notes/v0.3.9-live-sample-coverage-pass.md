# v0.3.9 Live Sample Coverage Pass

Release checkpoint for `xm-gold-ai-trader`, recording that the read-only live
sample coverage gate has reached PASS without changing trading logic, strategy
logic, safety checks, order routing, or AI annotation behavior.

## Status

- Live sample coverage reached PASS with `101` unique closed bars.
- Required minimum unique closed bars: `100`.
- Remaining closed bars: `0`.
- `python -m pytest` passes with `167` tests.
- `python scripts\check_live_sample_coverage.py --json` returns PASS.
- `python scripts\research_pipeline_verify.py --json` returns PASS.

## Pytest Legacy Isolation

- `tests/legacy/test_service_dashboard.py` is an old V3/V4 dashboard
  interactive test.
- The default pytest suite now excludes `legacy` and `tests/legacy`.
- `tests/test_xm_gold_pytest_isolation.py` locks this collection policy.
- `tests/legacy/README.md` explains the legacy dashboard test scope.

## Safety Status

- Default `configs/xm_gold_ai_trader.demo.yaml` remains
  `execution.allow_order_send: false`.
- No Phase 3 research or AI script calls `order_check` or `order_send`.
- `orders_sent` remains `0` across the research verifier.
- AI annotations remain read-only commentary and cannot alter trading
  decisions.
- No martingale, no grid, and no lot increase after loss.

## Verification Commands

```powershell
python -m pytest
python scripts\check_live_sample_coverage.py --json
python scripts\research_pipeline_verify.py --json
```
