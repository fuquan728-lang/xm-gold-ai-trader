# v0.4.1 Block Reason Attribution

Release checkpoint for `xm-gold-ai-trader`, extending the read-only live sample
quality report with block reason attribution.

This checkpoint does not change trading logic, strategy thresholds, safety
checks, order routing, or AI annotation behavior.

## Scope

- Extended `scripts/analyze_live_sample_quality.py`.
- Added explicit block reason attribution fields:
  - total journals
  - unique closed bars
  - block count
  - final signal count
  - candidate signal count
  - block reason counts and percentages
  - multi-reason combinations
  - candidate-to-final rejection reasons
  - per-campaign block reason distribution
  - zero-final-signal attribution as dominant rule vs distributed filters
- Added pytest coverage for block reason percentages, multi-reason
  combinations, candidate rejection attribution, and per-campaign distribution.

## Current Findings

Using the current live dry-run sample set:

- total journals: `109`
- valid journals: `109`
- unique closed bars: `101`
- final signal count: `0`
- candidate signal count: `1`
- block count: `101`
- block reason counts:
  - `NO_ACTIONABLE_SIGNAL`: `100` (`99.01%`)
  - `LOT_BELOW_VOLUME_MIN`: `1` (`0.99%`)
- candidate-to-final rejection:
  - `BUY` candidate rejected by `LOT_BELOW_VOLUME_MIN`
- zero final signal attribution:
  - classification: `DOMINANT_RULE`
  - dominant reason: `NO_ACTIONABLE_SIGNAL`
  - dominant share: `99.01%`

This is a research-quality finding only. It does not alter the baseline
strategy, risk controls, execution routing, or AI annotation boundary.

## Safety Status

- Default `configs/xm_gold_ai_trader.demo.yaml` remains
  `execution.allow_order_send: false`.
- `orders_sent` remains `0`.
- `order_check_called` remains `false`.
- `order_send_called` remains `false`.
- No Phase 3 research or AI script calls `order_check` or `order_send`.
- AI annotation remains read-only commentary.
- No martingale, no grid, and no lot increase after loss.

## Verification Commands

```powershell
python -m pytest
python scripts\analyze_live_sample_quality.py --json
python scripts\research_pipeline_verify.py --json
```
